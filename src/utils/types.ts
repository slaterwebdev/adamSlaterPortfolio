export type GridItem = {
  id: string;
  content?: string;
  url?: string;
  src?: string;
};

export type WorkExperience = {
  yearsServed: string;
  id: string;
  jobTitle: string;
  content: string;
};