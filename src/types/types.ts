export type Experience = {
  yearsServed: string;
  companyName: string;
  jobTitle: string;
  description: string;
};

export type GridItem = {
  icon?: JSX.Element;
  content?: string;
  url?: string;
  imageSrc?: string;
};
